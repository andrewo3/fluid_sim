#version 430
layout(local_size_x = 1, local_size_y = 1) in;
layout(r32f, binding = 0) uniform image2D vx;
layout(r32f, binding = 1) uniform image2D vy;


void diffuse(int N, int b, )


void main() {
    // base pixel color for image
    addForces();
    diffuse();
    move();
}